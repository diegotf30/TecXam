<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <%= link_to "TecXam", root_path, class: 'navbar-brand' %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li id="exams" class="nav-item active">
        Examenes
      </li>
      <li id="questions" class="nav-item active">
        Preguntas
      </li>
    </ul>
    <% if current_user %>
      <span class="navbar-text bold">
        <%= button_to "Salir", destroy_user_session_path, method: :delete, class: "btn btn-danger bold", id: "sign_out" %>
      </span>
    <% end %>
  </div>
</nav>
